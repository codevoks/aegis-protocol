[package]
name = "aegis-math"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "no_std, float-free, Solana-independent fixed-point arithmetic primitives for Aegis"
license = "Apache-2.0"

[dependencies]

[dev-dependencies]
proptest = "1"
num-bigint = "0.5"
num-traits = "0.2"
